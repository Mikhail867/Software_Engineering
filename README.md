# Тема 3. Операторы, условия, циклы
Отчет по Теме #3 выполнил(а):
- Глушков Михамл Сергеевич
- ПИЭ-21-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + | |
| Задание 7 | + | |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

# Лабараторные работы 
   ## Лабараторная работа 1
Создайте две переменные, значение которых будете вводить через консоль. Также составьте условие, в котором созданные ранее переменные будут сравниваться, если условие выполняется, то выведете в консоль «Выполняется», если нет, то «Не выполняется».


  ```python
one = int(input("введите значение первой переменной"))
two = int(input("введите значение второй переменной"))
if one>= two:
    print("выполняеться")
else:
    print("не выполняеться")
```
  ### Результат
  
![1](https://github.com/Mikhail867/Software_Engineering/assets/144737787/2a58e544-9f1c-4c1a-98f8-96a59ec35af7)

 
## Краткий вывод:
Программа запрашивает у пользователя ввод значения для первой переменной, используя функцию input. Введенное пользователем значение будет преобразовано в целое число с помощью функции int и сохранено в переменной one.

Затем программа запрашивает у пользователя ввод значения для второй переменной, аналогично преобразовывает введенное значение в целое число и сохраняет его в переменной two.

Далее программа выполняет условную проверку: если значение переменной one больше или равно значению переменной two, то выводится сообщение "выполняется". В противном случае (когда значение one меньше значения two), выводится сообщение "не выполняется".


   ## Лабараторная работа 2
Напишите программу, которая будет определять значения переменной меньше 0, больше 0 и меньше 10 или больше 10. Это нужно реализовать при помощи одной переменной, значение которой будет вводится через консоль, а также при помощи конструкций if, elif, else.


  ```python

one = int(input("введите значение  переменной"))

if one<0:
    print("Переменная меньше 0")
elif 0<one<10 :
    print("Переменная больше 0 и меньше 10 ")
else:
    print("Переменная больше 10")
```
  ### Результат
  
![2](https://github.com/Mikhail867/Software_Engineering/assets/144737787/93addc62-cc56-4881-8810-48e5f7cc4443)

 
## Краткий вывод:
Программа запрашивает у пользователя ввод значения для переменной one, используя функцию input. Введенное пользователем значение будет преобразовано в целое число с помощью функции int и сохранено в переменной one.

Затем программа выполняет условную проверку:

Если значение переменной one меньше 0 (one < 0), то программа выводит сообщение "Переменная меньше 0".
Если значение переменной one больше 0 и меньше 10 (0 < one < 10), то программа выводит сообщение "Переменная больше 0 и меньше 10 ".
Если ни одно из вышеперечисленных условий не выполняется (то есть значение переменной one больше или равно 10), то программа выводит сообщение "Переменная больше 10".


   ## Лабараторная работа 3

Напишите программу, в которой будет проверяться есть ли переменная в указанном массиве используя логический оператор in. Самостоятельно посмотрите, как работает программа со значениями которых нет в массиве numbers.

  ```python
numbers = [1,3,4,6,8,9]
value = int(input("Введите значение переменной: "))
if value in numbers:
    print("Переменная есть в данном массиве ")
else:
    print("Переменная нет в данном массиве ")
```
  ### Результат
  
![3](https://github.com/Mikhail867/Software_Engineering/assets/144737787/2da37f97-1a78-4531-9186-7288e455afe9)

 
## Краткий вывод:

Программа создает список numbers, содержащий набор чисел: [1, 3, 4, 6, 8, 9].

Затем программа запрашивает у пользователя ввод значения для переменной value с помощью функции input. Введенное пользователем значение будет преобразовано в целое число с помощью функции int и сохранено в переменной value.

Далее программа выполняет условную проверку: она проверяет, присутствует ли значение переменной value в списке numbers, используя оператор in.

Если значение value присутствует в списке numbers, то программа выводит сообщение "Переменная есть в данном массиве".

Если значение value не найдено в списке numbers, то программа выводит сообщение "Переменная нет в данном массиве".



   ## Лабараторная работа 4
Напишите программу, которая будет определять находится ли переменная в указанном массиве и если да, то проверьте четная она или нет. Самостоятельно протестируйте данную программу с разными значениями переменной value


  ```python
numbers = [1,3,4,6,8,9,16,321]
value = int(input("Введите значение переменной: "))
if value in numbers:
    if value % 2 == 0:
        print("Переменная четная и есть в данном массиве ")
    else:
        print("Переменная нечетная и есть в данном массиве ")
else :
    print(f"Переменная нет в данном массиве  и она равна {value}")
```
  ### Результат
  
![4](https://github.com/Mikhail867/Software_Engineering/assets/144737787/3693f2b8-8b32-4fae-82d1-9e98cd3e6125)

 
## Краткий вывод:

Программа создает список numbers, содержащий набор чисел: [1, 3, 4, 6, 8, 9, 16, 321].

Затем программа запрашивает у пользователя ввод значения для переменной value с помощью функции input. Введенное пользователем значение будет преобразовано в целое число с помощью функции int и сохранено в переменной value.

Далее программа выполняет условную проверку:

Сначала она проверяет, присутствует ли значение value в списке numbers с помощью оператора in.
Если значение value присутствует в списке numbers, программа переходит к следующему вложенному условию.
Во вложенном условии программа проверяет, является ли значение переменной value четным или нечетным, используя оператор % (остаток от деления на 2):

Если значение value четное (то есть value % 2 == 0), программа выводит сообщение "Переменная четная и есть в данном массиве".
В противном случае (когда value нечетное), программа выводит сообщение "Переменная нечетная и есть в данном массиве".
Если значение value не найдено в списке numbers (то есть первое условие не выполняется), программа выводит сообщение "Переменная нет в данном массиве и она равна [значение переменной value]".

 
   ## Лабараторная работа 5
    Напишите программу, в которой циклом for значения переменной i будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и операций работают в цикле.


  ```python
for i in range(10):
    print("i =", i)
    if i== 0:
        i += 2
    if i == 1:
        continue
    if i == 2 or i==3:
        print("переменная равна 2 или 3")
    elif i in [4,5,6]:
        print("переменная равна 4,5 или 6")
    else:
        break
```
  ### Результат
  
![5](https://github.com/Mikhail867/Software_Engineering/assets/144737787/e84ab1b2-5a9d-4379-804a-f1e4301bbf62)

 
## Краткий вывод:

Цикл for i in range(10): начинается и итерируется от 0 до 9 включительно.

На каждой итерации цикла программа выводит значение переменной i с помощью print("i =", i).

Первое условие if i == 0: проверяет, равно ли значение i нулю. Если i равно 0, то выполняется инструкция i += 2, что увеличивает значение i на 2. Важно отметить, что изменения значения i не влияют на текущий цикл, и цикл все равно перейдет к следующей итерации.

Далее идет условие if i == 1:, которое проверяет, равно ли значение i 1. Если i равно 1, то программа выполняет оператор continue, который прерывает текущую итерацию цикла и переходит к следующей итерации. В результате, код ниже этой точки на текущей итерации не будет выполняться.

Затем есть условие if i == 2 or i == 3:, которое проверяет, равно ли значение i 2 или 3. Если это условие выполняется, программа выводит сообщение "переменная равна 2 или 3".

Если ни одно из предыдущих условий не выполняется, программа переходит к условию elif i in [4, 5, 6]:, которое проверяет, присутствует ли значение i в списке [4, 5, 6]. Если i равно одному из этих значений, программа выводит сообщение "переменная равна 4, 5 или 6".

Если ни одно из условий выше не выполняется (то есть значение i не соответствует ни одному из условий), программа выполняет оператор break, который завершает выполнение цикла for досрочно. Таким образом, цикл завершается, и выполнение программы продолжается после цикла.


   ## Лабараторная работа 6
Напишите программу, в которой при помощи цикла for определяется есть ли переменная value в строке string и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной value оказалось в строке string.


  ```python
string = "Привет всем изучающим Python"
value = input("Введите букву: ")

found = False  

for index, char in enumerate(string):
    if char == value:
        print(f"Буква {value} есть в строке под индексом {index}")
        found = True
        break

if not found:
    print(f"Буква {value} нет в указанной строке")
```
  ### Результат
  
![6](https://github.com/Mikhail867/Software_Engineering/assets/144737787/adc663a3-e026-4547-9c22-487e586f1fc2)

 
## Краткий вывод:
Проблема в  коде заключается в том, что он всегда выводит сообщение "Буква {value} нет в указанной строке" на каждой итерации цикла for, даже если буква value найдена в строке. Чтобы исправить это,  можно  использовать флаг (логическую переменную), чтобы отслеживать, была ли буква найдена в строке
Мы вводим значение для переменной value.

Используется флаг found, который изначально устанавливается в False, чтобы указать, что буква value еще не была найдена.

Мы используем цикл for для перебора символов в строке и проверяем, равен ли текущий символ value.

Если символ value найден, мы выводим сообщение о нахождении буквы и ее индексе, а также устанавливаем флаг found в True и выходим из цикла с помощью break.

После цикла проверяем значение флага found. Если буква не была найдена (found остался False), выводим сообщение о том, что буква не найдена в указанной строке.

   ## Лабараторная работа 7

Напишите программу, в которой вы наглядно посмотрите, как работает цикл for проходя в обратном порядке, то есть, к примеру не от 0 до 10, а от 10 до 0. В уже готовой программе показано вычитание из 100, а вам во время реализации программы будет необходимо придумать свой вариант применения обратного цикла.

  ```python
for i in range(10, -1, -1):
    square = i ** 2
    print(f"Число {i} в квадрате равно {square}")

```
  ### Результат
  
![7](https://github.com/Mikhail867/Software_Engineering/assets/144737787/de9efe3b-f586-425c-80e4-94c3d5626f71)

 
## Краткий вывод:

range(10, -1, -1) создает последовательность чисел, начиная с 10 и заканчивая -1 (исключительно), с шагом -1, что позволяет нам итерироваться в обратном порядке.

На каждой итерации цикла мы вычисляем квадрат числа i и выводим его, используя f-строку для форматирования вывода.


   ## Лабараторная работа 8
Напишите программу используя цикл while, внутри которого есть какие-либо проверки, но быть осторожным, поскольку циклы while при  неправильно написанных условиях могут становится бесконечными, как указано в примере далее.


  ```python
value = 0
while value < 100:
    if value == 0:
        value += 10
    elif value // 5 > 1:
        value *= 5
    else:
        value += 5
    print(value)

```
  ### Результат
  
![8](https://github.com/Mikhail867/Software_Engineering/assets/144737787/2ec93162-73e1-4efd-a436-1747d800306b)


 
## Краткий вывод:

Инициализируется переменная value со значением 0.

Запускается цикл while, который будет выполняться до тех пор, пока значение переменной value меньше 100.

Внутри цикла проверяется текущее значение value:

Если value равно 0, то добавляется 10 к его значению (value += 10).
Если результат целочисленного деления value // 5 больше 1 (то есть value больше или равно 10), то value умножается на 5 (value *= 5).
Если ни одно из вышеперечисленных условий не выполняется (то есть value меньше 10), то к value добавляется 5 (value += 5).
На каждой итерации цикла программа выводит текущее значение value.

Таким образом, программа будет выполняться и изменять значение value на каждой итерации в соответствии с условиями, а затем выводить измененное значение. Процесс будет продолжаться до тех пор, пока value не станет больше или равным 100.


   ## Лабараторная работа 9

Напишите программу с использованием вложенных циклов и одной проверкой внутри них. Самое главное, не забудьте, что нельзя использовать одинаковые имена итерируемых переменных, когда вы используете вложенные циклы

  ```python
value = 0
for i in range(10):
    for j in range(10):
        if i != j:
            value += j
        else:
            pass
print(value)
```
  ### Результат
  
![9](https://github.com/Mikhail867/Software_Engineering/assets/144737787/01a153ee-4539-4c2f-a40b-59beee51b4b6)

 
## Краткий вывод:

Инициализируется переменная value со значением 0.

Запускается внешний цикл for, который будет выполняться 10 раз, изменяя значение переменной i от 0 до 9.

Для каждого значения i внутри внешнего цикла запускается вложенный цикл for, который также будет выполняться 10 раз, изменяя значение переменной j от 0 до 9.

Внутри вложенного цикла проверяется условие if i != j:. Если i не равно j, то выполняется следующее действие: значение переменной j прибавляется к переменной value. Это означает, что на каждой итерации внутреннего цикла, где i не равно j, значение j добавляется к value.

Если i равно j, то выполняется блок else, который содержит pass. В этом случае ничего не происходит, и программа переходит к следующей итерации внутреннего цикла.

После завершения внутреннего цикла for j in range(10):, программа переходит к следующей итерации внешнего цикла for i in range(10):.

В конечном итоге, значение переменной value будет равно сумме всех значений j, которые были добавлены в value на итерациях, где i не равно j.

После завершения внешнего цикла, программа выводит значение value, которое будет суммой значений от 0 до 9, исключая значение i, так как для i == j ничего не добавляется к value

   ## Лабараторная работа 10

Напишите программу с использованием flag, которое будет определять есть ли нечетное число в массиве. В данной задаче flag выступает в роли индикатора встречи нечетного числа в исходном массиве, четных чисел.

  ```python
numbers = [2, 4, 6, 8, 10]


has_odd = False


for num in numbers:
    if num % 2 != 0:
        has_odd = True
        break


if has_odd:
    print("В массиве есть нечетное число.")
else:
    print("В массиве нет нечетных чисел.")

```
  ### Результат
  
![10](https://github.com/Mikhail867/Software_Engineering/assets/144737787/34cfeab7-5632-45eb-b203-32d7743e0993)

 
## Краткий вывод:

Создается исходный массив numbers, который содержит четные числа.

Инициализируется флаг has_odd значением False, предполагая, что в массиве нет нечетных чисел.

Затем программа проходит по элементам массива с помощью цикла for и проверяет каждое число с помощью оператора % на нечетность (num % 2 != 0). Если найдено хоть одно нечетное число, флаг has_odd устанавливается в True, и цикл прерывается с помощью оператора break.

После завершения цикла, программа проверяет значение флага has_odd и выводит соответствующее сообщение.

Таким образом, программа определяет наличие нечетных чисел в массиве с использованием флага.
# Самостоятельные работы

## Самостоятельная работа №1

Напишите программу, которая преобразует 1 в 31. Для выполнения поставленной задачи необходимо обязательно и только один раз использовать: • Цикл for • *= 5 • += 1 Никаких других действий или циклов использовать нельзя

 ```python
result = 1

for _ in range(5):
    result *= 5
    result += 1

print(result)

```
  ### Результат
  
![11](https://github.com/Mikhail867/Software_Engineering/assets/144737787/118b18fb-1234-418f-81c1-a1458e31ff72)

 
## Краткий вывод:
result инициализируется значением 1.
Цикл for выполняется 5 раз (всего 5 итераций), так как range(5) создает последовательность от 0 до 4 включительно.
На каждой итерации значение result умножается на 5 (result *= 5), что эквивалентно result = result * 5.
К полученному результату добавляется 1 (result += 1), что эквивалентно result = result + 1.
Цикл повторяется 5 раз, каждый раз увеличивая значение result.
После завершения цикла выводится значение result.


## Самостоятельная работа №2

Напишите программу, которая фразу «Hello World» выводит в обратном порядке, и каждая буква находится в одной строке консоли.

 ```python
phrase = "Hello World"
for char in reversed(phrase):
    print(char)

```
  ### Результат
  
![12](https://github.com/Mikhail867/Software_Engineering/assets/144737787/f4bb8c59-2ef8-4753-ad19-952d5cf0742f)

 
## Краткий вывод:
phrase = "Hello World" - создается строковая переменная phrase со значением "Hello World".

for char in reversed(phrase): - начинается цикл for, который проходит по символам строки phrase, причем символы берутся в обратном порядке.

print(char) - на каждой итерации цикла текущий символ char выводится на экран


## Самостоятельная работа №3

Напишите программу, на вход которой поступает значение из консоли, оно должно быть числовым и в диапазоне от 0 до 10 включительно (это необходимо учесть в программе). Если вводимое число не подходит по требованиям, то необходимо вывести оповещение об этом в консоль и остановить программу. Код должен вычислять в каком диапазоне находится полученное число. Нужно учитывать три диапазона: • от 0 до 3 в от 3 до 6 • от 6 до 10 включительно Результатом работы программы будет выведенный в консоль диапазон. Программа должна занимать не более 10 строчек в редакторе кода. 

 ```python
try:
    number = float(input("Введите число от 0 до 10: "))
    if 0 <= number <= 10:
        if 0 <= number <= 3:
            print("Число находится в диапазоне от 0 до 3.")
        elif 3 < number <= 6:
            print("Число находится в диапазоне от 3 до 6.")
        else:
            print("Число находится в диапазоне от 6 до 10.")
    else:
        print("Число не входит в указанный диапазон.")
except ValueError:
    print("Ошибка ввода. Введите числовое значение.")
```
  ### Результат
  
![13](https://github.com/Mikhail867/Software_Engineering/assets/144737787/3e3ae1d0-d0fd-44c2-b37e-1ce60c68b696)

 
## Краткий вывод:
В блоке try программа пытается выполнить следующие действия:

Запросить у пользователя ввод числа с плавающей точкой в диапазоне от 0 до 10.
Попытаться преобразовать введенное значение в число с плавающей точкой (float()).
Если введенное значение является числом с плавающей точкой, программа проверяет, находится ли это число в заданном диапазоне от 0 до 10 (включительно).

В зависимости от результата проверки, программа выводит соответствующее сообщение в консоль:

Если число входит в диапазон от 0 до 3, выводится сообщение "Число находится в диапазоне от 0 до 3."
Если число входит в диапазон от 3 до 6, выводится сообщение "Число находится в диапазоне от 3 до 6."
Если число входит в диапазон от 6 до 10, выводится сообщение "Число находится в диапазоне от 6 до 10."
Если число не входит в указанный диапазон, выводится сообщение "Число не входит в указанный диапазон."
Если в блоке try происходит ошибка (например, если пользователь ввел нечисловое значение), выполнение переходит к блоку except, и выводится сообщение "Ошибка ввода. Введите числовое значение."



## Самостоятельная работа №4
Манипулирование строками. Напишите программу на Python, которая принимает предложение (на английском) в качестве входных данных от пользователя. Выполните следующие операции и отобразите результаты: • Выведите длину предложения. • Переведите предложение в нижний регистр. • Подсчитайте количество гласных (a, e, i, o, u) в предложении. • Замените все слова "ugly" на "beauty". •Проверьте, начинается ли предложение с "The" и заканчивается ли на "end". Проверьте работу программы минимум на 3 предложениях, чтобы охватить проверку всех поставленных условий

 ```python
def process_sentence(sentence):
    
    print("Длина предложения:", len(sentence))


    sentence_lower = sentence.lower()
    print("Предложение в нижнем регистре:", sentence_lower)


    vowels = "aeiou"
    vowel_count = sum(1 for char in sentence_lower if char in vowels)
    print("Количество гласных в предложении:", vowel_count)


    sentence_replaced = sentence.replace("Привет", "Здравствуйте")
    print("Предложение после замены слов:", sentence_replaced)


    starts_with_the = sentence.startswith("начало")
    ends_with_end = sentence.endswith("конец")
    print("Начинается с 'начало':", starts_with_the)
    print("Заканчивается на 'конец':", ends_with_end)

# Примеры использования программы
sentence1 = "Привет, почему ты долго не выходил на связь?"
process_sentence(sentence1)

sentence2 = "Привет, как отдохнула?"
process_sentence(sentence2)

sentence3 = "Привет, я думала, что больше тебя никогда не увижу. конец"
process_sentence(sentence3)

```
  ### Результат
  
![14](https://github.com/Mikhail867/Software_Engineering/assets/144737787/bdf0f8a1-f733-47c2-88d8-bc27868fad5b)

 
## Краткий вывод:

print("Длина предложения:", len(sentence)) - выводится длина введенного предложения.

sentence_lower = sentence.lower() - создается новая строка sentence_lower, в которой все буквы приводятся к нижнему регистру.

print("Предложение в нижнем регистре:", sentence_lower) - выводится предложение в нижнем регистре.

vowels = "aeiou" - создается строка vowels с гласными буквами.

vowel_count = sum(1 for char in sentence_lower if char in vowels) - подсчитывается количество гласных букв в предложении.

print("Количество гласных в предложении:", vowel_count) - выводится количество гласных букв.

sentence_replaced = sentence.replace("Привет", "Здравствуйте") - заменяются все вхождения "Привет" на "Здравствуйте".

print("Предложение после замены слов:", sentence_replaced) - выводится предложение после замены.

starts_with_the = sentence.startswith("начало") - проверяется, начинается ли предложение с "начало".

ends_with_end = sentence.endswith("конец") - проверяется, заканчивается ли предложение на "конец".

print("Начинается с 'начало':", starts_with_the) - выводится результат проверки начала предложения.

print("Заканчивается на 'конец':", ends_with_end) - выводится результат проверки конца предложения.

## Самостоятельная работа №5
Составьте программу, результатом которой будет данный вывод в консоль:
 ```python
memory = " world "
string = " hello"
values = [0, 2, 4, 6, 8, 10]
counter = 0

if counter not in values:
    while " world" not in string:

        string + "world"


    if counter > 7:

        print( memory)
        

while counter != 10:
    if counter < 10:
        counter += 1
        print(string)
        print(string + memory)
```
  ### Результат
  
![15](https://github.com/Mikhail867/Software_Engineering/assets/144737787/e42f4f02-aee3-482e-abd1-14ff9bc03afd)

 
## Краткий вывод:
memory = " world " - создается строковая переменная memory со значением " world ".
string = " hello" - создается строковая переменная string со значением " hello".
values = [0, 2, 4, 6, 8, 10] - создается список values с заданными значениями.
counter = 0 - устанавливается начальное значение переменной counter в 0.
Блок условия:

if counter not in values: - если текущее значение counter не содержится в списке values, выполняется следующий блок кода.
while " world" not in string: - цикл while, который продолжается до тех пор, пока подстрока " world" не появится в переменной string.
string + "world" - это выражение не изменяет переменную string. Должно быть string += "world", чтобы добавить "world" к переменной string.
Блок после цикла:

if counter > 7: - если counter больше 7, выполняется следующий блок кода.
print(memory) - выводится значение переменной memory.
Далее идет второй цикл:

while counter != 10: - второй цикл while, который продолжается до тех пор, пока counter не станет равным 10.
if counter < 10: - если counter меньше 10, выполняется следующий блок кода.
counter += 1 - увеличивается значение counter на 1.
print(string) - выводится значение переменной string.
print(string + memory) - выводится конкатенированное значение переменных string и memory
Но также я поменял строчку  if counter  in values: на  if counter  not in values: потому что если текущее значение counter не содержится в списке values, выполняется следующий блок кода.
# Общий вывод:
программы демонстрируют использование различных операторов, условий и циклов в языке программирования Python:

Операторы:

Используются операторы вывода (print) для вывода информации в консоль.
Применяется оператор присваивания (=) для создания переменных и сохранения значений.
Условия:

Вводятся условные конструкции if, elif и else для проверки определенных условий.
Проверяется длина предложения и выполняются действия в зависимости от результата.
Циклы:

Используется цикл for для итерации по символам строки и выполнения определенных операций.
Применяется цикл for для повторения процесса обработки нескольких предложений.
Реализуется цикл while для ввода значений от пользователя до тех пор, пока не будет введено корректное значение.
Операции со строками:

Используются методы строк, такие как lower(), replace(), startswith(), endswith(), для манипуляций с текстом.
Ввод и обработка данных:

Программы запрашивают ввод данных от пользователя с использованием input().
Предусмотрены проверки на корректность ввода, такие как обработка исключений (ValueError).
 Программы демонстрируют использование операторов, условий и циклов для обработки строк и ввода данных от пользователя в Python. Они предоставляют примеры эффективного использования различных конструкций языка для решения задач по манипуляции строками и контролю потока программы.
